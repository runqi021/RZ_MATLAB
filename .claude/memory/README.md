# Claude Code session memory — snapshot

A copy of the Claude Code memory directory for this project, committed so it
travels with the repo (rig computer <-> analysis computer).

**Live location** (where Claude actually reads and writes it):

    C:\Users\<user>\.claude\projects\C--Users-Admin-Desktop-RZ-MATLAB\memory\

On a new machine, copy this folder there. This copy is a SNAPSHOT and goes stale
as soon as the live one is updated — re-copy before committing.

`MEMORY.md` is the index loaded into context each session: one line per memory,
no content of its own.
