---
name: project_260615_whisk_breath_coordination
description: "260615 whisk DLC (Jun17, vL1/vL2/vR1/vR2) + atan2d angle switch, DLC_diag_RZ, breath×whisk overlay"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5b72e4e4-eb69-4350-aa72-7dd8848e4124
---

260615_thermalNbasler dual-cam session work (2026-06-17):

**Whisk DLC project:** `D:\260615_thermalNbasler\whisk\260615_whisk-RZ-2026-06-17` (trained on another PC; config.yaml `project_path`+`video_sets` were `C:\DLC\RZ\...` → repointed to `D:\...`; no other stale paths in project incl. pytorch_config.yaml). Bodyparts **vL1/vL2/vR1/vR2** where **1=base (near face), 2=tip** (user-confirmed). Whisker angle = `atan2d(tip-base)` per side.

**whisker_DLC_hilbert_RZ.m updated:** tracking now consistently good → switched `swMode='angle'` (was 'proj'). Columns map L0=vL1(2-3,base), L1=vL2(5-6,tip), R0=vR1(8-9), R1=vR2(11-12) — same positions as old project so whiskangle() (atan2+unwrap, deg) works unchanged. Use **atan2d NOT atand** (atand divides dy/dx → blows up at vertical, folds to ±90). See [[project_whisker_signal_choices]] (this supersedes the earlier eig-projection default).

**DLC_diag_RZ.m** (repo root): pooled keypoint-likelihood % histogram for a DLC csv folder + per-bodypart median/%<0.6 bars; run on thermal nose folder and whisk folder. MATLAB version of [[reference_dlc_likelihood_qc]]-style check.

**breath_whisk_overlay_RZ.m** (repo root): pair a session by animal+runIdx; load thermal `_breath.mat` (see [[project_thermal_nostril_roi_breathing]]) + compute whisk L/R atan2d angle; plot stacked (true units) + z-scored overlay. Cameras ~co-triggered 400Hz but clocks not strictly synced → rough time-from-0 overlay only ("for now"); tight sync needs a shared signal.

**Sync video — thermal_basler_sync_video.py** (flir env: fnv+cv2+numpy): side-by-side BASLER whisker avi (DLC whisker pts overlaid: vL1/vR1 base dot, vL2/vR2 tip ring, line, L=green R=cyan) | FLIR thermal in bluewhitered LUT. Both video parts + timestamps.csv live IN the run folder `D:\260615_thermalNbasler\<animal>\cam1_*\` (the cam1_*.avi = basler 816x486 400Hz; Rec-*.ats = thermal 96x60). CLOCKS NOT COMPARABLE: basler wall=Unix 2026, FLIR frame_info.time reads 1976 (bogus) → align by time-from-first-frame using each cam's own per-frame ts (handles thermal drops, ~268 short). EXPOSURE COMPENSATION: effective = ts + exp/2; basler exp 0.25ms (+0.125), FLIR integration 0.97648ms (+0.488) → FLIR 0.363ms later (sub-frame at 400Hz). whisk csv auto-found by animal+runIdx (run folder position among sorted cam1_*). Params WINDOW/OUT_FPS/FRAME_STEP. OPEN: thermal shown raw (flipped cam) — may need fliplr to match basler L/R; START_OFFSET if cams don't start same trigger.

UPDATES (2026-06-18): thermal panel now = SINGLE 30x30 crop at the MIDPOINT between the two nostrils (CROP_WIN=30, follows L_center/R_center from run-folder _nostrilC.mat; colormap window = 2-98 pct of the crop so breathing modulation is visible). make_sync(run_folder) refactored + SEQUENTIAL read (seek once + grab-skip, much faster than per-frame seek). Output renamed `<date>_<animal>_n<k>_sync_video.mp4`. **thermal_basler_sync_batch.py** loops all run folders with a _nostrilC.mat. BATCHED all 17 sessions (full clip, FRAME_STEP=2, OUT_FPS=100) = ~6.4GB. The ONLY time shift applied is exposure-center comp (basler +0.125ms, FLIR +0.488ms → net thermal +0.363ms, sub-frame); NO START_OFFSET. Apparent thermal-lags-whisking is mostly PHYSICAL (nostril thermal inertia ~tens of ms), not a sync bug.

**Epoch tools (2026-06-18):** `whisk_epoch_gui_RZ.m` — pick a few clean whisking epochs per session on the trace (3 linked panels: envelope+threshold guide & epoch patches, filtered L/R whisk + breath, Hilbert phase of all 3). NO auto-detect (starts empty), NO good/bad. Keys a/e/d add/adjust/delete, ←→ review, s save, [ ] jump session (auto-saves, resumes from saved). Saves `<date>_<animal>_n<k>_whisk_epochs.mat` (field `epochs` Nx2 s). Build list = all cam1_* with a whisk csv. `whisk_epoch_analyze_RZ.m` — pools samples inside the saved epochs (all sessions or ONLY_FOLDER) → 3x3: amplitude scatters (xL/xR, breath/xL, breath/xR), phase-diff polar+PLV (L-R, br-L, br-R), phase scatters (phiL/phiR, phiB/phiL, phiB/phiR); breath resampled to whisk 400Hz grid before Hilbert. Whisk feature: BP=[5 30] → Hilbert envelope; breath BR_BP=[1 15]. Sync video whisker offset bug FIXED: each animal's basler avi is a DIFFERENT resolution (5916297 816x486, 5916296 688x439, 5916300 656x466, 5840027 752x512) — sync now uses native res (was hardcoded 816x486 → stretched coords).

**Epoch analysis + videos (2026-06-18 v2):** `whisk_epoch_analyze_auto_RZ.m` (WHISK, env>thr) + `whisk_epoch_analyze_quiet_RZ.m` (QUIET, env<thr) — both share a `pool_epochs(detectFn,...)` engine; thr=0.10*pctile(env,95), MIN_DUR=1s, MERGE_GAP=0.2s. In-epoch samples split by instantaneous BREATH RATE (from breath Hilbert phase, `instfreq_phase`): BASELINE (<SNIFF_THR=6Hz, BLACK) vs FAST sniff (≥6Hz, RED). **FIG1 phase** 3x6: row1 amplitude scatters (overlaid), row2 Δφ stair-hists (L-R | L-br | R-br, 0=insp peak, title also gives Δt ms = μ/2πf_med), row3 phase scatters baseline|sniff SIDE BY SIDE (overlaid 2D clouds unreadable → split). **FIG2 time** 2x3: insp-triggered avgs breath/whiskL/whiskR (±STD NOT SEM — user: "sem is bullshit"; t=0=insp peak via phiB upward-zero-cross, TRIG_WIN=±0.6s + axis margins so curve isn't cropped) | L-R whisk xcorr | breathing PSD | whisking PSD. PSD = per-epoch pwelch (NFFT 1024, ham 256) averaged within class, EPOCH classed slow/fast by its MEDIAN breath rate, log-y with ±1 STD (linear→asymmetric on log), peak-freq in legend. (insp-triggered whisk-ENVELOPE panel + whisk-breath-env xcorr were REMOVED per user.) **FIG3** angle distribution: whisker L/R BP-angle histograms, baseline(black) vs sniff(red), σ in title. Row1 amplitude scatters now 6 SEPARATE base|sniff panels (xL/xR, breath/xL, breath/xR) each with linear fit + pair locked to shared xy-lims. Verified run: WHISK base 390s/4.7Hz + sniff 426s/7.1Hz; QUIET base 610s/4.5Hz + sniff 89s/6.6Hz. Measured: 17 sess, 1357s rec → WHISK 207 ep/820s, QUIET 203 ep/703s. WHY xcorr alone is wrong for breath↔whisk: different carrier freqs → oscillating/decaying xcorr; phase/PLV is the right cross-freq tool (xcorr OK only for same-freq L-R, or on slow envelopes). `thermal_basler_epoch_video.py` (flir env) — 20x-slow sync reels: detects whisk/quiet epochs IN PYTHON (scipy butter3+filtfilt+hilbert, EXACT match to MATLAB 207/203), writes ONE concatenated reel per set per session (full 400fps→OUT_FPS=20, black label card between epochs, reuses thermal_basler_sync_video panel/overlay). Per-session concat only (basler res differs per animal). ~24KB/frame → whisk ~7-8GB, quiet ~6-7GB (~14GB both); RAM negligible (streaming). 20x slow = lower out_fps, keeps every frame, does NOT inflate file size. See [[project_thermal_basler_sync_260613]].

**Analysis finalized (2026-06-19):** auto + quiet scripts are now IDENTICAL engine (only detect env>thr vs <thr + labels differ). Bands standardized: whisk **BP=[6 30]**, breath **BR_BP=[2 15]** across all whisk/breath coordination + diag scripts. Classification = **per-breath 1/ITI** (inspiration onsets = troughs of BP breath; each sample tagged with its breath's 1/ITI), **quiet/basal <5 Hz vs sniff >7 Hz, 5-7 dropped** (replaced the old Hilbert single-6Hz). FIG1 row2 = **TIME-lag histograms** (Δt=wrap(Δφ)/2πf, per-sample whisk rate for L-R, breath rate for vs-breath; 0=insp peak) NOT phase. FIG2 = insp-trig whisk L/R + L-R xcorr + breath/whisk PSD; **PSD = per-epoch pwelch of the BANDPASSED signals (xL/xR, brw), LINEAR power, arithmetic mean±STD, linear y** (user iterated: pwelch not plain-fft/multitaper; tried log-space then reverted to linear — log made the filter-skirt floor look big). FIG3 = angle distribution. Result: WHISK ep basal 215s/sniff 253s; QUIET ep basal 476s/sniff 37s → sniffing co-occurs with whisking. Diagnostics built: `breath_hilbert_peak_diag_RZ.m` (raw vs BP Hilbert-peak overlay — raw detrended gives ~30/s noise, MUST bandpass), `whisk_detect_diag_RZ.m` (whisk envelope+threshold+epochs & protraction onsets; BP[6 30] gave 48% whisking vs [4 60]→80%), `whisk_breath_detect_diag_RZ.m`, `whisk_breath_freq_hist_RZ.m` (1/ITI hist, basal/sniff classify), `whisk_breath_amp_freq_RZ.m` (per-breath amp vs freq: sniff faster AND lower amp 0.149 vs 0.204°C), `whisk_breath_raster_RZ.m` (protraction onset vs inspiration, sorted by breath dur, window=next-next insp), `whisk_breath_trig_avg_RZ.m` (insp-trig breath basal vs sniff, ±SEM). NOTE: Hilbert envelope/phase needs NARROWBAND — raw broadband Hilbert is garbage.

**Coordinates (user):** head faces anterior = reference. Thermal camera mounted flipped BUT tracked-point labels were pre-flipped, so L1=left/L2=right is anatomically correct AND coords match the raw .ats (confirmed: breath extraction yields clean signals). Breath signal is L+R average = flip-invariant anyway. Whisk angle is demeaned/z-scored so orientation only sets protraction sign.
